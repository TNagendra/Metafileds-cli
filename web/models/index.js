// const { Sequelize, DataTypes } = require('sequelize');

// const sequelize = new Sequelize(process.env.DB_NAME, process.env.DB_USER, process.env.DB_PASSWORD, {
//   host: process.env.DB_HOST,
//   dialect: 'postgres',
// });

// // Import models
// const ProductModel = require('./Product.js')(sequelize, DataTypes);
// const VariantModel = require('./Variant')(sequelize, DataTypes);
// const MetafieldModel = require('./Metafield')(sequelize, DataTypes);

// // Define associations if needed
// // For example:
// // ProductModel.hasMany(VariantModel);

// module.exports = {
//   Product: ProductModel,
//   Variant: VariantModel,
//   Metafield: MetafieldModel,
// };
